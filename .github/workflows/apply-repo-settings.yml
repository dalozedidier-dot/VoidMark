name: Mises à jour (repo settings)

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/apply-repo-settings.yml"

jobs:
  apply-repo-settings:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder (non bloquant)
        run: |
          echo "apply-repo-settings: workflow volontairement neutre."
          echo "Si tu veux automatiser des réglages GitHub: ajouter un script et/ou une action dédiée."
